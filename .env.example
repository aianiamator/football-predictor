# Copy to .env if you need to override defaults. .env is gitignored.
#
# There are NO API keys in this project. The app reads static JSON from
# Cloudflare, so there is no service to authenticate against.

# Where the SQLite store lives. Defaults to data/forecasts.db.
# FORECAST_DB=/srv/football-predictor/data/forecasts.db
