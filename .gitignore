# Secrets - never commit
.env
.env.*
!.env.example

# Python
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# Data + outputs
*.db
*.db-wal
*.db-shm
data/raw/
data_cache/
output/
data/*.json
# ...except the team-name mapping, which is source, not output. CI cannot
# settle a single match without it.
!data/team_aliases.json
*.log

# Node / app
node_modules/
app/dist/
app/.env
app/.env.*
!app/.env.example

# Backtest outputs
backtest_summary.csv
backtest_predictions.csv

# Dev-only seeded data, never published
app/public/data/
data/dev_forecasts.db*

# TS build info
*.tsbuildinfo

# Model tuning artefacts
backtest_ridge_*.csv
